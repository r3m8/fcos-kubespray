# fcos-kubespray

## Requirements

- Create libvirt network
- Create SSH key
- Review variables on top of the script

## Create Python virtual environment

```bash
cd kubespray/
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
